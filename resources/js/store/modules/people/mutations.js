export const SET_ALL = (state, people) => {
    state.people = people
}

export const SET_PERSON = (state, person) => {
    state.person = person
}

export const SET_PERSON_INDEX = (state, index) => {
    state.person_index = index
}

export const SET_FILTERS = (state, filters) => {
    state.filters = filters
}
